import { HeaderBox } from './style';

const Header = () => {
  return (
    <HeaderBox>
      <button>icon</button>
      <button>list</button>
      <button>Mushroom AI</button>
    </HeaderBox>
  );
};

export default Header;
